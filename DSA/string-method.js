// String method: -
// length, replace, startsWith, endsWith, concat, slice, uppercase, lowercase, charCodeAt, char, 
// repeat, includes, substr, 

let str = 'programming';
let s = 'localhost:3001/api/auth apple';
let val;

// val  = str.length;
// val = str.replace(/[a-zA-Z]/g, 'a'); // aaaaaaaaaa
// val = s.startsWith('l'); // true
// val = str.endsWith('g'); // true
// val = str.slice(1); // 'rogramming'
// val = str.slice(-1);  // g
// val = str.concat(` ${s}`); // programming localhost:3001/api/auth
// val  = str.charAt(5) // 'a'
// val  = str.charCodeAt(5);  // 97
// val = str.repeat(2); // programmingprogramming
// val = str.includes('r'); // true
// val = s.substring('apple');  // localhost:3001/api/auth apple


// console.log(val);