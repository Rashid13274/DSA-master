// Delete an element from the array.

//  using filter 
function deleteElement(){
    let arr = [1,3,5,6,11,23,10];
    let k = 6;

    //  using filter
    // arr = arr.filter((ele) => ele !== k);
    }
    deleteElement();
